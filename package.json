{
	"name": "mind-bloom",
	"version": "0.0.0",
	"private": true,
	"workspaces": [
		"packages/*",
		"packages/common/*"
	],
	"scripts": {
		"dev:api": "npm run dev -w @mindbloom/api",
		"db:generate": "npm run generate -w @mindbloom/database",
		"db:push": "npm run db:push -w @mindbloom/database",
		"db:migrate": "npm run migrate -w @mindbloom/database"
	}
}
