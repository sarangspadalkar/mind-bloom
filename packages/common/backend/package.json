{
	"name": "@mindbloom/common-backend",
	"version": "1.0.0",
	"description": "Shared backend utilities for MindBloom (auth, helpers)",
	"main": "src/index.ts",
	"scripts": {
		"typecheck": "tsc --noEmit"
	}
}
