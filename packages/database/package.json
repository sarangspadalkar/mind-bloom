{
	"name": "@mindbloom/database",
	"version": "1.0.0",
	"description": "Prisma database client and schema for MindBloom",
	"main": "src/index.ts",
	"scripts": {
		"generate": "prisma generate",
		"db:push": "prisma db push",
		"migrate": "prisma migrate dev",
		"studio": "prisma studio"
	},
	"dependencies": {
		"@prisma/client": "^6.6.0"
	},
	"devDependencies": {
		"prisma": "^6.6.0"
	}
}
